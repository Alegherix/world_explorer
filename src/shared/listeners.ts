// Event listeners -> Need to be kept somewhere where they can be referenced
// To make sure we properly unmount them to prevent memory leaks, aswell as
// Having multiple inputs being registered when component is remounted
// For example, In scenarios where player go back to the main screen to switch World
